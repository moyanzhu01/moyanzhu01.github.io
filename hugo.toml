baseURL = "https://moyanzhu01.github.io/"
languageCode = "en"
title = "Moyan Zhu"
theme = "PaperMod"

enableRobotsTXT = true
buildDrafts = false
buildFuture = false

[params]
  defaultTheme = "auto"
  description = "Ph.D. Candidate in Finance"
  
  [params.homeInfoParams]
    Title = "Hi, I'm Moyan Zhu 👋"
    Content = """
    Ph.D. Candidate in Finance at University of International Business and Economics.
    
    Focusing on Empirical Asset Pricing, and Market Micro-structure.
    """
    imageUrl = "img/avatar.png"     # 这里填你刚才拖进去的头像文件名
    imageLayout = "same"            # 图片居中 (可选: "profile" 并排显示, "same" 或其他值居中显示)
    imageWidth = 300                # 图片宽度（像素），默认 150
    imageHeight = 400               # 图片高度（像素），默认 150

[params.label]
text = "Moyan Zhu"      # 网站左上角显示的文字（通常写你的名字）

  [params.assets]
    # 浏览器标签页图标（Favicon）
    favicon = "img/favicon.png"              # 默认图标（可以是 .ico 或 .png）
    favicon16x16 = "img/favicon.png"          # 16x16 像素图标
    favicon32x32 = "img/favicon.png"          # 32x32 像素图标
    apple_touch_icon = "img/favicon.png"      # Apple 设备图标（建议 180x180）
    safari_pinned_tab = "img/favicon.png"     # Safari 固定标签页图标（SVG 格式）
    theme_color = "#2e2e33"                # 主题颜色（浏览器地址栏颜色）
    msapplication_TileColor = "#2e2e33"   # Windows 磁贴颜色

[[menu.main]]
  identifier = "about"
  name = "About"
  url = "/about/"
  weight = 10

[[menu.main]]
  identifier = "research"
  name = "Research"
  url = "/research/"
  weight = 20

[[menu.main]]
  identifier = "posts"
  name = "Blog"
  url = "/posts/"
  weight = 30

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true